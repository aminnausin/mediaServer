# Release 0.16.0-beta.5

**Release Date:** 2025-11-23

## Features

- move docker and github release workflows into individual files (``)
- workflows for 3 types of branches (``)
- vast improvement to progress bar time and visual accuracy (``)
- extract player timeline to custom component (``)
- add copy and save video frame options to video context menu (``)
- secure env based demo account for logs (``)
- show buffer on progress bar (``)
- load timestamp from url (``)
- show/hide full video description with clickable timestamps (``)
- tentative support for clickable timestamps in descriptions (``)
- add hour support to lyrics (``)
- allow local network access to vite dev (``)
- move trusted hosts to config (``)
- update readme with demo (``)
- basic suspense element on video titles (``)
- add app version info card to settings (``)
- show expected task times (``)
- add horizon button to tasks page (``)
- nginx and font caching (``)
- playback caching (``)
- demo auto reset db (``)
- add framerate to player stats (``)
- basic demo seeder (``)
- record and tag factories (``)
- optional login message (``)
- add placeholders on login and register (``)
- scan files command (``)
- queue password reset on user (``)
- queue password resets (``)
- cache website stats for 60 seconds (``)
- add redis to docker compose, fix websockets and add email to docker env (``)
- basic redis queue setup (``)
- add redis and horizon to docker compose file (``)
- add redis and horizon to docker compose file (``)
- install horizon (``)
- move shared env setup to script and add user config for domain (``)
- reduce docker image size slightly by generating manifest within the build stage and not shipping git installed (``)
- add redis and horizon to docker compose file (``)
- install horizon (``)
- add redis and horizon to docker compose file (``)
- install horizon (``)
- add redis and horizon to docker compose file (``)
- install horizon (``)
- add view count to readme (``)
- expose date file added to server (``)
- finish translating all pulse components to vue (``)
- folder id and artist/album on mediaSession (``)
- global modal (``)
- toast swipes (``)
- add breadcrumbs to settings and make spacing and structure consistent between settings and dashboard (``)
- delete account, sign out of other sessions and page height consistencies (``)
- auth form styling (``)
- docker test mailpit setup in env (``)
- generalise modal better (``)
- backend for deleting account (``)
- password reset (``)
- session delete backend (``)
- password reset backend (``)
- move auth forms to form components (``)
- major form styling rework (``)
- further updates to doc (``)
- updated readme and operation instructions (``)
- password reset skeleton (``)
- convert some old js to ts and remove files that are no longer used (``)
- convert clipboard to typescript (``)
- global loading bar and minimal scrollbar (this probably has bugs) (``)
- add link to settings from dashboard (``)
- settings page with working sessions, user email and password changes (``)
- general styling improvements and button style variants (``)
- mobile icon taken from laravel jetstream (``)
- generalise input label when not in form (``)
- centralise form error styling (``)
- root view with redirect logic to setup page (``)
- user sessions backend (``)
- Backend Support for changing Passwords and Emails with Testing (``)
- username skeleton in navbar (``)
- new lyrics editor and tests (``)
- lyrics api and updating service + tests and factories (``)
- php testing config (``)
- new form button (``)
- add album and artist fields to metadata (``)
- expand on button style variants for buttonIcon (``)
- testing commands and configuration with coverage (``)
- add search lyric api (``)
- fix dashboard styling on library cards and breadcrumbs (``)
- basic user profile (``)
- restructure lyrics preview and add UI indicating preview state (``)
- implement metadata media type instead of reading mime type (``)
- basic lyrics importer (``)
- temporary feature documentation (``)
- sorting test (``)
- new sorting strategy to fix episode sorting (not too sure about this) (``)
- lyrics styling and keybinds (``)
- lyric fading effects (``)
- live lyrics viewer (``)
- add lyric support but not allowing edits yet (``)
- add rating edit support and show it on preview (``)
- add media type to series and optimise generating it (``)
- video auto play option (``)
- codec info in video card (``)
- add codec info to video description panel (``)
- dashboard breadcrumbs (``)
- manifest extra info (``)
- version file (``)
- minimal manifest implementation (``)
- minimal horizontal scrollbar (``)
- select prefix (``)
- fonts and header hierarchy (``)
- fading ambient mode (``)
- category privacy toggle and fix logout redirect (``)
- updated docker with full feature set (``)
- docker compatibility and better cross platform compatibility with puppeteer (``)
- update docker setup with working websockets (``)
- health-check (``)
- generic table and folder table without search (``)
- generalise forms and allow folder tag editing (``)
- add tags to series (``)
- Folder tag backend structure and improved previews (``)
- better date picker (``)
- change preview content, compatibility for nixos, format values (``)
- generate preview images (``)
- better link preview style (``)
- better quality og previews (``)
- increase path resolver support (``)
- working on an SSR implementation for folder previews (``)
- Move job functionality to service and serviceProvider (``)
- listen to mediaPlayPause on videoPlayer (``)
- add folder name to mediasession (``)
- add index folders to main dropdown (``)
- code smells (``)
- failed attempt to add volume slider in popover based on device (``)
- live user counts using plausible (``)
- media session next/previous + autoplay (``)
- media session API (``)

## Bug Fixes

- changelog needs to commit to main and not prod (``)
- comments in docker release (``)
- docker and github publishing actions don't have secret access (``)
- new ci actions lack permissions (``)
- video card title blocks description hover card (``)
- hover card clips content if over height limit (``)
- video titles not clear when different from filename (``)
- user can drag video timeline, breaking progress and causing jumping (``)
- last active date on user card is invalid (``)
- api return data isnt type safe (``)
- unused admin check looks for a specific username (``)
- remove linted file from ci pint setup (``)
- video timeline is not rounded (``)
- lyrics edit and preview button should not show in fullscreen because modals or custom UI do not work in fullscreen (``)
- timeline flickering due to rounded borders (``)
- video timeline controls get hidden or shrink on mobile without hover events (``)
- heatmap does not show on hover (``)
- table search query reactivity is broken (``)
- redis jobs retry automatically early even if long jobs such as embed are still running and have not timed out via horizon (``)
- input label shows even if content is empty (``)
- video file title is not exposed on frontend and impossible to find if metadata titles are malformed (``)
- duplicate user-header id (``)
- folder card file count should reflect media type (``)
- user last active date is not updated (``)
- tasks run by non admin user dont send notification when finished (``)
- scrubbing inputs are buggy and get stuck (``)
- buffer doesnt appear on firefox (``)
- toggles use slider classes and duplicate them (``)
- verify error stack trace is lost when a new error with just a custom message is thrown (``)
- metadata composable should be computed and each property should recompute on its own (``)
- max height on hover card content breaks bottom padding (``)
- lyrics should scroll if timestamp is clicked in description (``)
- show more button should only appear when description overflows its max height (``)
- video and folder context menus have options when user isn't logged in, and the folder edit option opens the share modal (``)
- description should collapse when new video loaded (``)
- show more button is on description even if it isnt long enough to be expanded (``)
- missing default description (``)
- player buffer is disabled and doesn't show a real value (only shows number of buffer segments, not length in seconds) (``)
- metadata fields are not updated if file is updated (``)
- hover card isnt disableable and uses child position even if parent has overflow clip or hidden (``)
- readme INFO banner does not exist (``)
- copying a video with an embedded uuid to another folder without deleting the original causes duplicate key errors and is not checked for (``)
- folder is_majority_audio does not compare types correctly because of enums (``)
- other session ip address should not be visible on demo environments (``)
- ci-test shouldnt run in a pr from main to prod since every commit to main will already run tests through ci (``)
- relevant links on images are missing from readme (``)
- tentative performance fix (``)
- manifest crossorigin access issues in public folder (``)
- weird batch file variable and slash issue that causes random jumps (``)
- docker setup scripts error out when trying to create directories that already exist and add localhost url to finish message (``)
- docker nginx conf did not use custom logging (``)
- clear labels tag on secondary instances missing (``)
- lrclib service does not use album and artist correctly (``)
- metadata editor is titled for videos but should cover audio (``)
- show artist and album on editor when editing music and fix title inconsistencies (``)
- video card title has minimum width if no description is set (``)
- view counts don't reflect fake records in demo:reset (``)
- demo reset deletes everything, loses out on metrics and inefficiently recreates metadata instead of just clearing user submitted data (``)
- docker performance low with horizon on low end vps (``)
- composer lock file issues (``)
- login message should be in config not vite (``)
- faker is required for demo but not included in required composer packages (``)
- default docker redis host is wrong ip (``)
- default redis password not defined in docker env (``)
- ci change was on wrong job (``)
- .git folders aren't fetched when building docker file on github actions (``)
- docker healthcheck and manifest issues (``)
- passing class directly does not work on components with multiple root elements (``)
- manifest is not loaded by browser since it isnt public (``)
- manual class prop is applied on many components but vue applies it automatically (``)
- github icon doesnt show on manifest card (``)
- text and tags wrap on og preview (``)
- app manifest card does not sohw (``)
- suspense video info title is too bright in dark mode (``)
- date picker is not readonly and cannot set a new value from blank (``)
- extra margin on sidebar cards that misallign with content card (``)
- having waitTimes query in the dropdown menu causes the values to reset when navigating to a new page (``)
- attempt at reducing auth requests and error logs on log out (``)
- auth store allows concurrent fetch user requests (``)
- double data load on tasks and users, and toast does not reflect fetch result (``)
- uncaught error when profile doesn't exist (``)
- queries refetch on window focus (``)
- plausible doesnt react to spa routing (``)
- readme missing open graph preview and had typos (``)
- default docker env did not setup redis, and auto index job happened too often (``)
- lrclib search does not include current album and artist (``)
- task stats return no data (``)
- sync files local json file permission issues (``)
- update task returns error instead of null when task doesnt exist (``)
- scheduler should not run with node in production (``)
- npm php commands missing scheduler (``)
- lost writes race condition issue when concurrently updating tasks (``)
- task deletion errors (``)
- admin user can't use horizon outside of local env (``)
- vite env order reversed (``)
- nginx invalid referrer setup (``)
- no title on video card (``)
- docker frontend does not build as production (``)
- vite env not loaded (``)
- pulse cache card doesnt parse hits and misses (``)
- index files command uses old style (``)
- horizon requires extra command to update composer on unsupported platforms (``)
- edit library and edit folder dropdown overflows when names are long (``)
- scheduled index files uses old job setup and does not work (``)
- not using defined uid and gid when creating shared volume .init file (``)
- folder tags not eagar loaded (``)
- leaking if user email exists when trying to reset password (``)
- ambient background has width 0 if enabled after loading video (``)
- startDocker does not create the shared volume with correct permissions. Implement a workaround for the error codes (``)
- manifest not generating git tags on docker build (``)
- #159 empty categories list error and update setup page (``)
- app relies on removed volume prep container (``)
- empty lines copied over in default.conf incorrectly (``)
- docker setup batch file makes nginx config malformed (``)
- alias on internal docker network to allow external docker access to work (``)
- attempt to fix docker chrome startup issue (``)
- dockerfile chrome permissions are missing and reverb health check is incorrect (``)
- redirect guest still allowes logged in users if authstore was not hydrated (``)
- remove logging (``)
- login redirects to home rather than previous route (``)
- incorrect alias and data folder permissions for docker (``)
- duplicate directive in caddyfile (``)
- caddy config is not used correctly (``)
- caddy does not handle ws correctly in docker (``)
- sudo commands are in .bat instead of .sh (``)
- docker env comments counted as variables, and lack of app name config caused conflicts (``)
- log folders don't have permissions within the docker containers (``)
- non accessible containers have caddy labels (``)
- default files went into public instead of directly in data (``)
- release zip files output default data in wrong directory (``)
- rename caddyfile (``)
- rename CaddyFile (``)
- docker release has incorrect default files and config (``)
- ws events were broadcasted in instead of dispatched via queue, causing issues (``)
- const values in config don't work with cache (``)
- docker missing git for app manifest (should move this to a different stage) (``)
- node versions out of date with vulnerabilities in docker image builder (``)
- incorrect alt text on readme (``)
- lowercase label for views (``)
- url typo (``)
- context menu and hovercard use window.scrolly but I use body as the scroll container (``)
- duplicate "ago" in toTimeSpan on minutes (``)
- visible toasts are deleted as overflow even if they should stay visible (``)
- rebase to current branch not main when updating tags (``)
- setting branch tag on main does not work in ci (``)
- data refreshed message missing on tasks and users (``)
- trying to delete self user logs them out (``)
- version file (``)
- no gap between cards in mobile (``)
- part of page is hidden on mobile by toolbars (h-screen issue) (``)
- nested action needs permissions (``)
- cant use a relative action path until merged to main (``)
- release action uses old test jobs instead of optimised test jobs from ci-test (``)
- auto release doesnt tag current code (``)
- autolabel and type issues (``)
- multiple session expired errors (``)
- redirect guest checks for auth (``)
- no video error is logged twice (``)
- auth state is not loaded with app (``)
- layout padding on content card wrong (``)
- navbar initiates auth state (``)
- queries retry on auth failure (``)
- scroll to top when loading new folder was missing (``)
- padding for sidebar cards are duplicated on all instances vs centralised on layout (``)
- page minimum heights on mobile leave empty space and have a large gap for no reason (``)
- autolabel config has the wrong path (``)
- props sent to BaseModal which is not a single element but rather the header and the slot (``)
- second scrollbar shows up from hover cards Scroll Area #153 (``)
- video with black background bleeds around the edges even if video is the right aspect ratio (``)
- profile search api urls were misordered (``)
- toast notification text is not selectable (``)
- video card styling issues (``)
- auth form styling inconsistencies (``)
- session management and login complexity (``)
- dialog element inherits background colour (``)
- spacing on settings (``)
- failed login returns non-standard errors (``)
- login request has validation of email and password length, when it should just say invalid credentials (``)
- no video autoplay on firefox mobile causes player to never enter playable state (``)
- session controller namespace case (``)
- input text labelled has different padding to everything else (``)
- npm ci (``)
- checkout code in vue-test (``)
- ci-test takes too long and repeats steps (``)
- case sensitive session controller import (``)
- sessions test and code smells (``)
- unique emails are not enforced by laravel and cause 500 errors (``)
- autocomplete for form inputs are inconsistent (``)
- drop unique constraint on default folder id (``)
- drop unneeded unique (``)
- remove video_id from record as metadata covers everything now (``)
- incorrect lyrics servic query parameter names (``)
- link to profile in dropdown has wrong url to account (``)
- title on video card when codec and resolution are combined is missing (``)
- folder is_majority_audio is always true (``)
- test missing fields (``)
- vitest config used during build (``)
- missing jsdom types package (``)
- looking for jsdom types (``)
- missing jsdom (``)
- missing tsconfig options (``)
- files not included in vitest.json (``)
- ts files missing during build in ci env (``)
- vitest blocked in ci environment (``)
- verify files compares filemtime as an int to metadata->updated_at which is carbon and might be null (``)
- modal close button overlaps title (``)
- eslint and tooltip animations on video player and progress bar (``)
- variables declared as let and popover having a single word name (``)
- pinia storeToRefs returning any (``)
- types and code smells (``)
- make lyrics importer public (``)
- store types (``)
- video player tooltip overlaps when text changes (``)
- migration issue (``)
- sorting options should reflect folder media type (``)
- remove lyrics format requring decimals (``)
- public disk file permission issue with browsershot (``)
- lyric reset (``)
- show untimed lyrics (``)
- blur on non audio files when no lyrics present (``)
- last active date should be now and not today (``)
- incorect content on video title hover (``)
- video player time state lost when video has not loaded but was changed (``)
- dashboard styling (``)
- github release tag (``)
- syntax error on release action (``)
- avoid increasing patch number when pushing to dev directly (``)
- remove manifest code from actions (``)
- change versioning, return to 8086 default docker port and incorperate manifest (``)
- ci/cd permissions (``)
- pagination edge threshold at low values (``)
- pagination issues again (``)
- add video description to search filtering again (``)
- Styling and Contrast and Consistency Issues (``)
- dashboard tab issue (``)
- types (``)
- styles (``)
- layout changes (``)
- text colours and display user email (``)
- font colours (``)
- ambient player fade only when playing a video (``)
- update mobile ui for video player (``)
- fading ambient player with picture in picture mode (``)
- video info truncating (``)
- reduce player click areas (``)
- sidebar padding and scroll gutter bg colour (``)
- attempt at fixing home page document title (let the page handle it and skip in the router) (``)
- reorganise pulse util functions and fix graph max value (``)
- scan files has incorrect task counts due to missed field sub_tasks_current (``)
- task ended not updating tasks list frontend (``)
- slightly more video player padding (``)
- pagination wrapping and limit on max visible pages (``)
- image preview styling makes text container small if the thumbnail is large. Tags also overflow pushing the date too far or wrapping text (``)
- floor timespans and therefore fix user activity timespan (``)
- video cls shift and pause while loading bugs (``)
- pagination issues (``)
- remove debugging (``)
- table pagination overflow (``)
- chip tag shadow clipping (``)
- update dropdown menu and add library scan button (``)
- logging permissions (``)
- preview generator chrome profile (``)
- handle duplicate multi chip items with case difference (``)
- video player end time not updating (``)
- toast notification text overflow (``)
- ports on ws container (``)
- permission issues (``)
- preview generator job (``)
- history view types (``)
- history view types (``)
- history view types (``)
- generate preview job fix and optimise websocket broadcast jobs (``)
- disable focus scroll into view on multi select (``)
- update og image generation, always generate on dev server and display folders with audio wording when audio files are the majority (``)
- og preview style change (``)
- docker check (``)
- preview style change (``)
- text styling (``)
- correct og image size (``)
- tags on preview template (``)
- remove default og image (``)
- optimise video search and use the right values (``)
- route fuzzy search issues and video search (``)
- input multichip checking for unset value (``)
- correct text wrapping on hover card (``)
- folder update (``)
- Was combining strings with a + in series update request (``)
- document title should reflect video and folder correctly now (``)
- sidebar card information missing (``)
- page title breaks state when returning to a folder from another page (``)
- history shows as -1s ago (``)
- keybinds showing on mobile (``)
- video tooltips at different heights (``)
- multiselect focus behaviour (``)
- video title always shows when fullscreen (``)
- video tooltip layering and popover item overlap (``)
- handle autoplay and media keys better when a media session has not started (``)
- multiselect navigation (``)
- single select navigation (``)
- session count errors when plausible is not setup (``)
- keep player state in sync when stateVideo is updated (``)
- play pause sync to video player (``)
- plausible env (``)
- types (``)
- unverified videos have null name in media session (``)
- remove old sorting functions from dashboard (``)
- scan on category card only verifies it (``)
- media session position state issues (``)
- input select and multi select scrolling and item selection issues (``)
- hide ambient mode toggle in light mode (``)
- picture in picture state not updated by browser controls (``)
- video does not get browser native enhancement features on hover (``)
- upload appstore change (``)
- media session artwork https issue (``)
- media session artwork https issue (``)
- media session artwork https issue (``)
- media session artwork https issue (``)
- use folder name as artist for audio (``)
- media session api artwork (``)
- keybinds not changing on fullscreen, video loading state not affected on seek, end time flashing invalid date while video loading, timeline tooltip not disappearing when controls timeout (``)
- ambient player canvas flashing at wrong size and not updating on paused video seek or new video selected (``)
- video tooltip goes out of bounds when the content changes sizes without mouse exit (``)
- correct getting video duration when indexing (``)
- temp dynamic keybinds (``)
- remove redundant title attributes (``)
- hide video button titles when using tooltip (``)
- video no longer rounded in full screen (``)
- blocker code smells, player autoseek time reset, double time elapsed update, tooltip offset, keybinds when slider focused (``)
- video tooltip on top of popover (``)
- extra gap in hover card (``)
- pass video button tooltip offset to tooltip (``)
- fullscreen video player padding for curved corner mobile screens (``)
- Use reverb KEY not APP_ID for frontend config (``)
- docker beta tag (``)

## Documentation

- versioning documentation (``)

