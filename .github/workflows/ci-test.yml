name: Code Test

on:
  pull_request:
    branches: [ 'dev', 'main', 'prod' ]

jobs:
  run-tests:
    permissions:
      contents: write
    strategy:
      fail-fast: true
    uses: aminnausin/mediaServer/.github/workflows/tests.yml@dev
