// Visible toasts amount
const VISIBLE_TOASTS_AMOUNT = 3;

// Viewport padding
const VIEWPORT_OFFSET = '24px';
const MOBILE_VIEWPORT_OFFSET = '16px';

// Default toast width
const TOAST_WIDTH = 0;

const TOAST_LIFE = 3000;

// Default gap between toasts
const DEFAULT_GAP = 16;

const SWIPE_THRESHOLD = 45;

const Z_STEP = 10;
const SCALE_STEP = 0.06;
const Y_OFFSET_STEP = 16;

export { Z_STEP, SCALE_STEP, Y_OFFSET_STEP, VISIBLE_TOASTS_AMOUNT, VIEWPORT_OFFSET, MOBILE_VIEWPORT_OFFSET, TOAST_LIFE, TOAST_WIDTH, DEFAULT_GAP, SWIPE_THRESHOLD };
